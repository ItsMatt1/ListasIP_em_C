#include <stdio.h>

int main()
{
    int N = 0, M = 0;

    scanf("%d", &N);

    for (int i = 0; i < N; i++)
    {
        scanf("%d", &M);

        for (int j = 0; j < M; j++)
        {
        }
    }

    return (0);
}